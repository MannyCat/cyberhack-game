name: Build Android APK

on:
  push:
    branches: [main, master]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  workflow_dispatch:

jobs:
  build:
    name: Build & Release APK
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.27.4'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Run analyzer
        run: |
          flutter analyze --no-fatal-infos --no-fatal-warnings 2>&1 | tee analyze_output.txt || true
          echo 'Analyzer step completed (non-fatal)'

      - name: Build APK (release)
        id: build
        run: |
          flutter build apk --release 2>&1 | tee build_output.txt
          EXIT_CODE=$?
          echo "=== Build exit code: $EXIT_CODE ==="
          echo "=== Searching for APK ==="
          find . -name '*.apk' -type f 2>/dev/null
          echo "=== build/app/outputs tree ==="
          find build/app/outputs -type f 2>/dev/null || echo "(no outputs dir)"
          exit $EXIT_CODE

      - name: Upload all logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            analyze_output.txt
            build_output.txt

      - name: Upload APK files as artifacts
        uses: actions/upload-artifact@v4
        with:
          name: CyberHack-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/heads/main') || startsWith(github.ref, 'refs/heads/master')
        with:
          tag_name: v1.1.${{ github.run_number }}
          name: CyberHack v1.1.${{ github.run_number }}
          body: |
            ## CyberHack — Многопользовательская онлайн-стратегия о хакерстве

            ### Что нового (v1.1.0):
            - Исправлен краш при запуске приложения (ProGuard правила для Supabase)
            - Авто-создание профиля при первой авторизации
            - Защита от пустых данных (никней, ID узлов)
            - Улучшена обработка ошибок подключения к Supabase
            - Реальная карта мира с 18 городами (flutter_map + CartoDB Dark Matter)
            - Чат с Supabase Realtime (глобальный + клан-канал)
            - Система кланов: создание, вступление, управление
            - Чёрный рынок: магазин + инвентарь
            - 6 типов атак: DDoS, Malware, Phishing, Brute Force, SQL Injection, Zero Day
            - Рейтинг с 4 категориями и подиумом ТОП-3
            - Полностью русский интерфейс

            Автоматическая сборка из коммита `${{ github.sha }}`
          files: build/app/outputs/flutter-apk/app-release.apk
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
